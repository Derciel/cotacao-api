{
    "name": "estoquepro-monorepo",
    "private": true,
    "workspaces": [
        "cotacao-backend"
    ],
    "scripts": {
        "build:all": "npm run build:all --workspace=cotacao-backend",
        "start": "npm run start:prod --workspace=cotacao-backend",
        "start:prod": "npm run start:prod --workspace=cotacao-backend"
    },
    "dependencies": {
        "@nestjs/jwt": "^11.0.2",
        "@nestjs/passport": "^11.0.5",
        "@types/bcrypt": "^6.0.0",
        "@types/passport-jwt": "^4.0.1",
        "bcrypt": "^6.0.0",
        "passport": "^0.7.0",
        "passport-jwt": "^4.0.1"
    }
}
