---
import Layout from '../layouts/MainLayout.astro';
---

<Layout title="Usuários & Permissões | Nicopel Cargo">
  <div class="page-usuarios">
    <div class="header-section">
      <div class="title-group">
        <h1><i class="fas fa-users-cog"></i> Usuários & Permissões</h1>
        <p>Gerencie quem pode acessar o sistema e quais módulos estão disponíveis para cada perfil.</p>
      </div>
      <button id="btn-new-user" class="btn-primary">
        <i class="fas fa-user-plus"></i> Novo Usuário
      </button>
    </div>

    <div class="users-card glass-card animate-fade-in" id="users-container">
      <div class="loading-state">
        <i class="fas fa-circle-notch fa-spin"></i>
        <span>Sincronizando base de usuários...</span>
      </div>
    </div>

  <!-- Modal Novo/Editar Usuário -->
  <div id="user-modal" class="modal">
    <div class="modal-content glass-premium">
      <div class="modal-header">
        <h2 id="modal-title">Novo Usuário</h2>
        <button class="close-modal">&times;</button>
      </div>
      <form id="user-form">
        <input type="hidden" id="user-id" name="id" />
        
        <div class="modal-body-scroll" id="modal-body-content">
          <!-- O conteúdo será alternado entre o Form e a Visualização via JS -->
          <div id="form-container">
          <div class="form-section ux-square">
            <h3 class="section-title"><i class="fas fa-info-circle"></i> Dados de Acesso</h3>
            <div class="form-group">
              <label for="username">Nome de Usuário</label>
              <input type="text" id="username" name="username" required placeholder="Ex: joao.silva" />
            </div>
            <div class="form-grid-inner">
              <div class="form-group">
                <label for="role">Papel Principal</label>
                <select id="role" name="role" required>
                  <option value="SELLER">Vendedor</option>
                  <option value="ADMIN">Administrador</option>
                </select>
              </div>
              <div class="form-group" id="password-group">
                <label for="password">Senha</label>
                <input type="password" id="password" name="password" placeholder="Mínimo 6 caracteres" />
              </div>
            </div>
            <p id="pw-hint" class="hint-text">Deixe em branco para manter a senha atual ao editar.</p>
            
            <div class="form-actions-inline">
              <button type="submit" class="btn-primary-inline">
                <i class="fas fa-save"></i> Salvar Alterações
              </button>
            </div>
          </div>

          <div class="form-section ux-square">
            <h3 class="section-title"><i class="fas fa-shield-alt"></i> Permissões de Acesso (Páginas)</h3>
            <div class="permissions-grid">
              <label class="permission-card">
                <input type="checkbox" name="permissions" value="/"> 
                <div class="perm-card-content">
                  <div class="perm-icon"><i class="fas fa-home"></i></div>
                  <div class="perm-info">
                    <span class="perm-name">Dashboard</span>
                    <span class="perm-desc">Visão geral</span>
                  </div>
                </div>
              </label>
              <label class="permission-card">
                <input type="checkbox" name="permissions" value="/nova-cotacao"> 
                <div class="perm-card-content">
                  <div class="perm-icon"><i class="fas fa-plus-circle"></i></div>
                  <div class="perm-info">
                    <span class="perm-name">Nova Cotação</span>
                    <span class="perm-desc">Criação</span>
                  </div>
                </div>
              </label>
              <label class="permission-card">
                <input type="checkbox" name="permissions" value="/historico"> 
                <div class="perm-card-content">
                  <div class="perm-icon"><i class="fas fa-history"></i></div>
                  <div class="perm-info">
                    <span class="perm-name">Histórico</span>
                    <span class="perm-desc">Consulta</span>
                  </div>
                </div>
              </label>
              <label class="permission-card">
                <input type="checkbox" name="permissions" value="/relatorios"> 
                <div class="perm-card-content">
                  <div class="perm-icon"><i class="fas fa-chart-line"></i></div>
                  <div class="perm-info">
                    <span class="perm-name">Analytics</span>
                    <span class="perm-desc">Estatísticas</span>
                  </div>
                </div>
              </label>
              <label class="permission-card">
                <input type="checkbox" name="permissions" value="/usuarios"> 
                <div class="perm-card-content">
                  <div class="perm-icon"><i class="fas fa-users-cog"></i></div>
                  <div class="perm-info">
                    <span class="perm-name">Usuários</span>
                    <span class="perm-desc">Acessos</span>
                  </div>
                </div>
              </label>
              <label class="permission-card restriction-card">
                <input type="checkbox" name="permissions" value="quota_restricted"> 
                <div class="perm-card-content">
                  <div class="perm-icon"><i class="fas fa-user-lock"></i></div>
                  <div class="perm-info">
                    <span class="perm-name">Visão Restrita</span>
                    <span class="perm-desc">Apenas suas cotações</span>
                  </div>
                </div>
              </label>
            </div>
          </div>
        </div>
        <div id="view-container" style="display: none;"></div>
      </div>

        <div class="modal-footer">
          <button type="button" class="btn-secondary close-modal">Cancelar</button>
          <button type="submit" class="btn-primary" id="save-user-btn">Salvar Usuário</button>
        </div>
      </form>
    </div>
  </div>

  <script is:inline>
    const usersContainer = document.getElementById('users-container');
    const userModal = document.getElementById('user-modal');
    const userForm = document.getElementById('user-form');
    const btnNewUser = document.getElementById('btn-new-user');
    const closeModalBtns = document.querySelectorAll('.close-modal');
    const modalTitle = document.getElementById('modal-title');
    const pwHint = document.getElementById('pw-hint');
    const formContainer = document.getElementById('form-container');
    const viewContainer = document.getElementById('view-container');
    const modalFooter = userModal.querySelector('.modal-footer');

    // Mapeamento de nomes de permissões
    const PERM_NAMES = { 
      '/': 'Dashboard', 
      '/nova-cotacao': 'Cotação', 
      '/historico': 'Histórico', 
      '/relatorios': 'Analytics', 
      '/usuarios': 'Usuários',
      'quota_restricted': 'Restrito' 
    };

    async function loadUsers() {
      try {
        const res = await window.safeFetch('/api/users');

        if (res.ok) {
          renderUsers(res.data);
        } else {
          usersContainer.innerHTML = '<div class="error-state">Erro ao carregar usuários.</div>';
        }
      } catch (error) {
        usersContainer.innerHTML = '<div class="error-state">Falha na conexão com o servidor.</div>';
      }
    }

    function renderUsers(users) {
      if (users.length === 0) {
        usersContainer.innerHTML = `
          <div class="empty-state">
            <i class="fas fa-user-slash"></i>
            <h3>Nenhum usuário encontrado</h3>
            <p>Comece adicionando um novo colaborador ao sistema.</p>
          </div>
        `;
        return;
      }

      usersContainer.innerHTML = `
        <div class="table-responsive">
          <table class="data-table">
            <thead>
              <tr>
                <th>Usuário</th>
                <th>Papel</th>
                <th>Permissões</th>
                <th class="text-right">Ações</th>
              </tr>
            </thead>
            <tbody>
              ${users.map(user => {
                const permissions = user.permissions || [];
                return `
                <tr>
                  <td>
                    <div class="user-identity">
                      <div class="avatar-circle">${user.username.charAt(0).toUpperCase()}</div>
                      <div class="user-meta">
                        <span class="username">${user.username}</span>
                        <span class="user-id">#${user.id}</span>
                      </div>
                    </div>
                  </td>
                  <td>
                    <span class="role-badge ${user.role.toLowerCase()}">
                      ${user.role === 'ADMIN' ? 'Administrador' : 'Vendedor'}
                    </span>
                  </td>
                  <td>
                    <div class="tags-group">
                      ${permissions.slice(0, 3).map(p => `<span class="tag">${PERM_NAMES[p] || p}</span>`).join('')}
                      ${permissions.length > 3 ? `<span class="tag-more">+${permissions.length - 3}</span>` : ''}
                      ${permissions.length === 0 ? '<span class="tag-none">Mínima</span>' : ''}
                    </div>
                  </td>
                  <td class="text-right">
                    <div class="action-btns">
                      <button class="action-btn view" onclick='window.handleViewUser(${JSON.stringify(user)})' title="Ver Detalhes">
                        <i class="fas fa-eye"></i>
                      </button>
                      <button class="action-btn edit" onclick='window.handleEditUser(${JSON.stringify(user)})' title="Editar">
                        <i class="fas fa-pen"></i>
                      </button>
                    </div>
                  </td>
                </tr>
              `;}).join('')}
            </tbody>
          </table>
        </div>
      `;
    }

    window.handleViewUser = (user) => {
      const perms = (user.permissions || []).map(p => `
        <div class="view-perm-card ux-square-mini ${p === 'quota_restricted' ? 'restriction-active' : ''}">
          <div class="perm-icon-small"><i class="${p === '/' ? 'fas fa-home' : p === '/nova-cotacao' ? 'fas fa-plus-circle' : p === '/historico' ? 'fas fa-history' : p === '/relatorios' ? 'fas fa-chart-line' : p === 'quota_restricted' ? 'fas fa-user-lock' : 'fas fa-users-cog'}"></i></div>
          <span>${PERM_NAMES[p] || p}</span>
        </div>
      `).join('') || '<p class="tag-none">Sem permissões específicas atribuídas.</p>';
      
      viewContainer.innerHTML = `
        <div class="user-profile-view animate-fade-in">
          <div class="profile-card ux-square">
            <div class="profile-header-premium">
              <div class="profile-avatar-large shadow-glow">${user.username.charAt(0).toUpperCase()}</div>
              <div class="profile-title-group">
                <h3>${user.username}</h3>
                <span class="role-badge-large ${user.role.toLowerCase()}">
                  <i class="fas fa-${user.role === 'ADMIN' ? 'crown' : 'user'}"></i>
                  ${user.role === 'ADMIN' ? 'Administrador' : 'Vendedor'}
                </span>
              </div>
            </div>
          </div>
          
          <div class="profile-info-grid">
            <div class="detail-card ux-square">
              <div class="detail-label"><i class="fas fa-fingerprint"></i> ID do Sistema</div>
              <div class="detail-value"><code>#${user.id}</code></div>
            </div>
            <div class="detail-card ux-square">
              <div class="detail-label"><i class="fas fa-shield-alt"></i> Status da Conta</div>
              <div class="detail-value status-active"><i class="fas fa-check-circle"></i> Ativo</div>
            </div>
          </div>

          <div class="profile-permissions-card ux-square">
            <div class="detail-label-large"><i class="fas fa-key"></i> Módulos com Acesso Habilitado</div>
            <div class="view-perms-grid-premium">${perms}</div>
          </div>
        </div>
      `;
      
      modalTitle.textContent = 'Perfil do Usuário';
      formContainer.style.display = 'none';
      viewContainer.style.display = 'block';
      modalFooter.style.display = 'none';
      userModal.style.display = 'flex';
    };

    window.handleEditUser = (user) => openModal(user);

    function openModal(user = null) {
      userForm.reset();
      userForm.querySelectorAll('input[type="checkbox"]').forEach(cb => cb.checked = false);
      
      // Default UI state
      formContainer.style.display = 'block';
      viewContainer.style.display = 'none';
      modalFooter.style.display = 'flex';

      if (user) {
        modalTitle.textContent = 'Configurações de Usuário';
        document.getElementById('user-id').value = user.id;
        document.getElementById('username').value = user.username;
        document.getElementById('role').value = user.role;
        pwHint.style.display = 'block';
        document.getElementById('password').required = false;

        if (user.permissions && Array.isArray(user.permissions)) {
          user.permissions.forEach(p => {
            const cb = userForm.querySelector(`input[value="${p}"]`);
            if (cb) cb.checked = true;
          });
        }
      } else {
        modalTitle.textContent = 'Criar Novo Usuário';
        document.getElementById('user-id').value = '';
        pwHint.style.display = 'none';
        document.getElementById('password').required = true;
        
        ['/', '/nova-cotacao', '/historico'].forEach(p => {
          const cb = userForm.querySelector(`input[value="${p}"]`);
          if (cb) cb.checked = true;
        });
      }
      userModal.style.display = 'flex';
    }

    userForm.onsubmit = async (e) => {
      e.preventDefault();
      const formData = new FormData(userForm);
      const data = {
        id: formData.get('id'),
        username: formData.get('username'),
        role: formData.get('role'),
        permissions: formData.getAll('permissions')
      };
      
      const pwd = formData.get('password');
      if (pwd) data.password = pwd;

      const isEdit = !!data.id;
      if (!data.password && !isEdit) {
         window.showToast('Defina uma senha para o novo usuário', 'error');
         return;
      }

      const submitBtns = userForm.querySelectorAll('button[type="submit"]');
      submitBtns.forEach(btn => {
        btn.disabled = true;
        btn.dataset.originalText = btn.innerHTML;
        btn.innerHTML = '<i class="fas fa-circle-notch fa-spin"></i> Processando...';
      });

      try {
        const res = await window.safeFetch(isEdit ? `/api/users/${data.id}` : '/api/users', {
          method: isEdit ? 'PATCH' : 'POST',
          headers: { 
            'Content-Type': 'application/json'
          },
          body: JSON.stringify(data)
        });

        if (res.ok) {
          window.showToast(`Usuário ${isEdit ? 'atualizado' : 'cadastrado'} com sucesso!`, 'success');
          userModal.style.display = 'none';
          loadUsers();
        } else {
          window.showToast(res.data.message || 'Erro ao salvar alterações', 'error');
        }
      } catch (error) {
        window.showToast('Erro de conexão com o servidor', 'error');
      } finally {
        submitBtns.forEach(btn => {
          btn.disabled = false;
          btn.innerHTML = btn.dataset.originalText || (btn.id === 'save-user-btn' ? 'Salvar Usuário' : 'Salvar Alterações');
        });
      }
    };

    btnNewUser.onclick = () => openModal();
    closeModalBtns.forEach(btn => btn.onclick = () => userModal.style.display = 'none');
    window.onclick = (e) => { if (e.target === userModal) userModal.style.display = 'none'; };

    loadUsers();
  </script>

  <style is:global>
    /* --- SCOPED TO PAGE-USUARIOS TO PREVENT LEAKS --- */
    .page-usuarios .header-section {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 40px;
    }

    .page-usuarios .title-group h1 {
      font-size: 2.2rem;
      font-weight: 900;
      color: #0f172a;
      margin-bottom: 8px;
      display: flex;
      align-items: center;
      gap: 15px;
      letter-spacing: -0.025em;
    }

    .page-usuarios .title-group h1 i { color: #2563eb; }
    .page-usuarios .title-group p { color: #64748b; font-size: 1.1rem; }

    /* Users Table Overhaul */
    .page-usuarios .users-card { padding: 0; overflow: hidden; border: none; background: transparent; box-shadow: none; min-height: auto; }
    
    .page-usuarios .table-responsive { 
      background: white; 
      border-radius: 28px; 
      border: 1.5px solid #e2e8f0; 
      padding: 10px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.04);
      overflow-x: auto;
    }

    .page-usuarios .data-table { width: 100%; border-collapse: collapse; }
    .page-usuarios .data-table th { 
      padding: 24px 20px; text-align: left; font-size: 0.75rem; font-weight: 900; color: #64748b; 
      text-transform: uppercase; letter-spacing: 1px; border-bottom: 2px solid #f8fafc;
      background: #fbfcfe;
    }
    .page-usuarios .data-table td { padding: 20px; border-bottom: 1px solid #f1f5f9; vertical-align: middle; }
    .page-usuarios .data-table tr:last-child td { border-bottom: none; }
    .page-usuarios .data-table tr:hover td { background: #f8fafc; }

    .page-usuarios .user-identity { display: flex; align-items: center; gap: 16px; }
    .page-usuarios .avatar-circle { 
      width: 48px; height: 48px; background: linear-gradient(135deg, #3b82f6, #2563eb); 
      color: white; border-radius: 16px; display: flex; align-items: center; justify-content: center; 
      font-weight: 900; font-size: 1.3rem; box-shadow: 0 4px 12px rgba(37, 99, 235, 0.2);
      border: 2px solid white;
    }
    .page-usuarios .user-meta { display: flex; flex-direction: column; gap: 2px; }
    .page-usuarios .username { font-weight: 800; color: #0f172a; font-size: 1rem; }
    .page-usuarios .user-id { font-size: 0.75rem; color: #94a3b8; font-weight: 600; font-family: monospace; }

    .page-usuarios .role-badge { 
      padding: 7px 14px; border-radius: 12px; font-size: 0.7rem; font-weight: 900; 
      display: inline-flex; align-items: center; gap: 8px; text-transform: uppercase;
      letter-spacing: 0.5px;
    }
    .page-usuarios .role-badge.admin { background: #fef3c7; color: #92400e; }
    .page-usuarios .role-badge.seller { background: #dcfce7; color: #166534; }
    .page-usuarios .role-badge.user { background: #dcfce7; color: #166534; }

    .page-usuarios .tags-group { display: flex; gap: 8px; flex-wrap: wrap; }
    .page-usuarios .tag { background: #f8fafc; color: #475569; padding: 5px 12px; border-radius: 10px; font-size: 0.7rem; font-weight: 800; border: 1.5px solid #e2e8f0; }
    .page-usuarios .tag-more { background: #e0e7ff; color: #4338ca; padding: 5px 10px; border-radius: 10px; font-size: 0.7rem; font-weight: 900; }
    .page-usuarios .tag-none { color: #cbd5e1; font-style: italic; font-size: 0.85rem; }

    .page-usuarios .action-btns { display: flex; gap: 10px; justify-content: flex-end; }
    .page-usuarios .action-btn { 
      width: 42px; height: 42px; border-radius: 14px; border: 1.5px solid #e2e8f0; 
      background: white; color: #64748b; cursor: pointer; transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1); 
      display: flex; align-items: center; justify-content: center; font-size: 1rem;
    }
    .page-usuarios .action-btn:hover { border-color: #2563eb; color: #2563eb; transform: translateY(-3px); box-shadow: 0 8px 15px rgba(37, 99, 235, 0.1); }
    .page-usuarios .action-btn.view:hover { color: #3b82f6; border-color: #3b82f6; background: #eff6ff; }
    .page-usuarios .action-btn.edit:hover { color: #f59e0b; border-color: #f59e0b; background: #fffbeb; }

    /* Modal Framework */
    .page-usuarios .modal {
      position: fixed; inset: 0; background: rgba(15, 23, 42, 0.6); backdrop-filter: blur(8px);
      z-index: 2000; display: none; justify-content: center; align-items: center;
      padding: 20px;
    }
    
    .page-usuarios .modal-content {
      background: white; width: 100%; max-width: 680px; 
      border-radius: 32px; box-shadow: 0 25px 50px -12px rgba(0,0,0,0.5);
      display: flex; flex-direction: column; max-height: 90vh;
      border: 1px solid rgba(255,255,255,0.1); overflow: hidden;
    }

    .page-usuarios .modal-header { padding: 32px 40px; display: flex; justify-content: space-between; align-items: center; border-bottom: 1px solid #f1f5f9; }
    .page-usuarios .modal-header h2 { font-size: 1.6rem; font-weight: 900; color: #0f172a; letter-spacing: -0.02em; }
    .page-usuarios .close-modal { background: #f1f5f9; border: none; width: 36px; height: 36px; border-radius: 50%; color: #64748b; font-size: 1.2rem; cursor: pointer; display: flex; align-items: center; justify-content: center; transition: 0.2s; }
    .page-usuarios .close-modal:hover { background: #fee2e2; color: #ef4444; transform: rotate(90deg); }

    .page-usuarios .modal-body-scroll { padding: 30px 40px; overflow-y: auto; flex: 1; scrollbar-width: thin; }
    
    .page-usuarios .modal-footer { 
      padding: 24px 40px; border-top: 1px solid #f1f5f9; background: #fbfcfe; 
      display: flex; justify-content: flex-end; gap: 16px; 
    }

    /* Buttons Premium */
    .page-usuarios .btn-primary { 
      background: linear-gradient(135deg, #004a99, #003366); color: white; border: none;
      padding: 14px 28px; border-radius: 16px; font-weight: 800; font-size: 0.95rem;
      cursor: pointer; transition: 0.3s; display: flex; align-items: center; gap: 10px;
      box-shadow: 0 10px 15px -3px rgba(0, 74, 153, 0.3);
    }
    .page-usuarios .btn-primary:hover { transform: translateY(-2px); filter: brightness(1.1); box-shadow: 0 15px 25px -5px rgba(0, 74, 153, 0.4); }
    .page-usuarios .btn-primary:disabled { opacity: 0.7; cursor: not-allowed; transform: none; box-shadow: none; }

    .page-usuarios .btn-primary-inline {
      background: #004a99; color: white; border: none;
      padding: 12px 24px; border-radius: 12px; font-weight: 800; font-size: 0.85rem;
      cursor: pointer; transition: 0.3s; display: flex; align-items: center; gap: 8px;
    }
    .page-usuarios .btn-primary-inline:hover { background: #003366; transform: translateY(-1px); }
    .page-usuarios .btn-primary-inline i { font-size: 0.9rem; }
    .page-usuarios .btn-primary-inline:disabled { opacity: 0.6; cursor: not-allowed; }

    .page-usuarios .form-actions-inline {
      display: flex; justify-content: flex-end; margin-top: 10px;
    }

    .page-usuarios .btn-secondary { 
      background: #f1f5f9; color: #475569; border: none; padding: 14px 24px; 
      border-radius: 16px; font-weight: 800; cursor: pointer; transition: 0.2s;
    }
    .page-usuarios .btn-secondary:hover { background: #e2e8f0; color: #1e293b; }

    /* Form Design UX Squares */
    .page-usuarios .form-section.ux-square { 
      margin-bottom: 25px; background: #f8fafc; padding: 30px; 
      border-radius: 24px; border: 1.5px solid #e2e8f0; transition: 0.3s;
    }
    .page-usuarios .form-section.ux-square:hover { border-color: #004a99; background: #fbfcfe; }
    .page-usuarios .section-title { font-size: 0.8rem; font-weight: 900; color: #004a99; margin-bottom: 25px; display: flex; align-items: center; gap: 10px; text-transform: uppercase; letter-spacing: 0.1em; }

    .page-usuarios .form-grid-inner { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; }
    .page-usuarios .form-group { display: flex; flex-direction: column; gap: 10px; margin-bottom: 20px; }
    .page-usuarios .form-group label { font-weight: 800; color: #334155; font-size: 0.85rem; padding-left: 4px; }
    .page-usuarios .form-group input, .page-usuarios .form-group select {
      padding: 14px 18px !important; border-radius: 16px !important; border: 1.5px solid #e2e8f0 !important; outline: none; transition: 0.2s; font-size: 1rem; color: #0f172a; background: white !important;
    }
    .page-usuarios .form-group input:focus, .page-usuarios .form-group select:focus { border-color: #004a99 !important; box-shadow: 0 0 0 4px rgba(0, 74, 153, 0.1) !important; }
    .page-usuarios .hint-text { font-size: 0.75rem; color: #94a3b8; margin-top: -10px; margin-bottom: 10px; padding-left: 4px; }

    /* Permissions Cards */
    .page-usuarios .permissions-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(180px, 1fr)); gap: 15px; }
    .page-usuarios .permission-card {
      display: flex; align-items: center; gap: 12px; padding: 15px; 
      background: white; border: 1.5px solid #e2e8f0; border-radius: 20px; 
      cursor: pointer; transition: 0.3s; position: relative;
    }
    .page-usuarios .permission-card:hover { border-color: #2563eb; transform: translateY(-2px); box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.05); }
    .page-usuarios .permission-card input[type="checkbox"] { 
      position: absolute; top: 15px; right: 15px; width: 22px; height: 22px; accent-color: #2563eb; margin: 0; z-index: 2;
    }
    .page-usuarios .permission-card:has(input:checked) { border-color: #2563eb; background: #f0f7ff; }
    .page-usuarios .permission-card:has(input:checked) .perm-icon { background: #2563eb; color: white; }
    
    .page-usuarios .perm-card-content { display: flex; align-items: center; gap: 15px; width: 100%; }
    .page-usuarios .perm-icon { width: 40px; height: 40px; background: #eff6ff; color: #2563eb; border-radius: 12px; display: flex; align-items: center; justify-content: center; font-size: 1.1rem; transition: 0.3s; }
    .page-usuarios .perm-info { display: flex; flex-direction: column; line-height: 1.3; }
    .page-usuarios .perm-name { font-weight: 850; color: #1e293b; font-size: 0.95rem; }
    .page-usuarios .perm-desc { font-size: 0.7rem; color: #64748b; font-weight: 600; text-transform: uppercase; letter-spacing: 0.5px; }

    /* Profile View Overhaul */
    .page-usuarios .user-profile-view { animation: slideUp 0.4s ease; display: flex; flex-direction: column; gap: 20px; }
    .page-usuarios .profile-card.ux-square { background: linear-gradient(135deg, #f8fafc, #eff6ff); border: 2px solid #dbeafe; padding: 25px !important; }
    .page-usuarios .profile-header-premium { display: flex; align-items: center; gap: 30px; }
    .page-usuarios .profile-avatar-large { 
      width: 100px; height: 100px; background: linear-gradient(135deg, #2563eb, #1d4ed8); 
      color: white; border-radius: 32px; display: flex; align-items: center; justify-content: center; 
      font-size: 3rem; font-weight: 900; border: 4px solid white; box-shadow: 0 10px 30px rgba(37, 99, 235, 0.3);
    }
    .page-usuarios .profile-title-group h3 { font-size: 2rem; font-weight: 900; color: #0f172a; margin-bottom: 8px; letter-spacing: -1px; }
    .page-usuarios .role-badge-large { 
      padding: 10px 20px; border-radius: 16px; font-size: 0.85rem; font-weight: 900; 
      display: inline-flex; align-items: center; gap: 10px; text-transform: uppercase;
    }
    .page-usuarios .role-badge-large.admin { background: #fef3c7; color: #92400e; }
    .page-usuarios .role-badge-large.seller, .page-usuarios .role-badge-large.user { background: #dcfce7; color: #166534; }

    .page-usuarios .profile-info-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; }
    .page-usuarios .detail-card { padding: 30px !important; display: flex; flex-direction: column; gap: 12px; }
    .page-usuarios .detail-label { font-size: 0.75rem; font-weight: 900; color: #64748b; text-transform: uppercase; display: flex; align-items: center; gap: 10px; }
    .page-usuarios .detail-value { font-size: 1.2rem; font-weight: 850; color: #334155; }
    .page-usuarios .detail-value code { background: #f1f5f9; padding: 5px 12px; border-radius: 10px; color: #2563eb; }
    .page-usuarios .status-active { color: #10b981; }

    .page-usuarios .detail-label-large { 
      font-size: 0.85rem; font-weight: 850; color: #1e293b; 
      margin-bottom: 25px; display: flex; align-items: center; gap: 12px; 
      text-transform: uppercase; letter-spacing: 0.5px;
    }
    .page-usuarios .detail-label-large i { color: #2563eb; background: #eff6ff; width: 32px; height: 32px; border-radius: 8px; display: flex; align-items: center; justify-content: center; }

    .page-usuarios .profile-permissions-card { padding: 35px !important; }
    .page-usuarios .view-perms-grid-premium { display: grid; grid-template-columns: repeat(auto-fill, minmax(180px, 1fr)); gap: 15px; }
    .page-usuarios .view-perm-card.ux-square-mini { 
      background: white; border: 2px solid #f1f5f9; padding: 15px 20px; border-radius: 20px; 
      display: flex; align-items: center; gap: 15px; transition: 0.3s;
    }
    .page-usuarios .perm-icon-small { width: 40px; height: 40px; background: #f0f7ff; color: #2563eb; border-radius: 12px; display: flex; align-items: center; justify-content: center; font-size: 1.2rem; }
    .page-usuarios .view-perm-card span { font-weight: 850; color: #1e293b; font-size: 0.95rem; }
    
    .page-usuarios .restriction-card:has(input:checked) { border-color: #f59e0b; background: #fffbeb; }
    .page-usuarios .restriction-card:has(input:checked) .perm-icon { background: #f59e0b; color: white; }
    .page-usuarios .restriction-active { border-color: #f59e0b !important; background: #fffbeb !important; }
    .page-usuarios .restriction-active .perm-icon-small { background: #f59e0b; color: white; }

    /* States & Feedback */
    .page-usuarios .loading-state, .page-usuarios .error-state, .page-usuarios .empty-state { padding: 80px 40px; display: flex; flex-direction: column; align-items: center; justify-content: center; color: #94a3b8; gap: 15px; text-align: center; }
    .page-usuarios .loading-state i { font-size: 2.5rem; color: #2563eb; margin-bottom: 15px; }
    .page-usuarios .empty-state h3 { color: #475569; font-weight: 900; }

    /* Animations */
    @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
    @keyframes slideUp { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }
    .page-usuarios .animate-fade-in { animation: fadeIn 0.5s ease; }

    @media (max-width: 768px) {
      .page-usuarios .header-section { flex-direction: column; align-items: flex-start; gap: 20px; }
      .page-usuarios .profile-header-premium { flex-direction: column; text-align: center; gap: 20px; }
      .page-usuarios .profile-info-grid { grid-template-columns: 1fr; }
      .page-usuarios .modal-content { max-height: 100vh; border-radius: 0; width: 100%; }
      .page-usuarios .view-perms-grid-premium { grid-template-columns: 1fr; }
    }
  </style>
</Layout>
